import {pricing as p,money,annualSaving,bundleReference,bundleSaving} from '../pricing-config.js';
import {softwareProducts,availableToolSlugs} from '../products.js';
const list=items=>`<ul class="price-inclusions">${items.map(x=>`<li>${x}</li>`).join('')}</ul>`;
export const priceJourney=()=>`<div class="value-journey" aria-label="Learn, apply, scale"><div><b>01 · Learn</b><span>${p.course.name}</span><strong>${money(p.course.launch)} one-time</strong></div><div><b>02 · Apply</b><span>Excel templates included with your course</span><strong>No additional cost</strong></div><div><b>03 · Scale · Optional</b><span>${p.suite.name}</span><strong>${money(p.suite.monthly)}/month or ${money(p.suite.annual)}/year</strong></div></div>`;
export const suitePricing=()=>`<div class="suite-price-options"><a href="purchase.html?offer=software&amp;billing=monthly"><b>${money(p.suite.monthly)}</b><span>/ month</span></a><a href="purchase.html?offer=software&amp;billing=annual"><span class="small-tag">Save ${annualSaving()}%</span><b>${money(p.suite.annual)}</b><span>/ year</span></a></div><p class="subtle">Example pricing · annual saving compared with 12 monthly payments. Subscriptions are not open yet.</p>`;
export const coursePricing=()=>`<section class="course-pricing-section" id="pricing"><span class="eyebrow">The course comes first</span><h2>Learn the method.<br>Keep the knowledge.</h2><div class="primary-price-card"><div><span class="small-tag">Launch price</span><h3>${p.course.name}</h3><p>Eight modules in the Planning System, from strategic foundation to funding and partnerships.</p><div class="course-price"><del aria-label="Regular price ${money(p.course.regular)}">${money(p.course.regular)}</del><strong>Launch price: ${money(p.course.launch)}</strong></div><p><b>One payment. Lifetime access to the course.</b></p><div class="price-actions"><a class="button" href="purchase.html?offer=course">Get the Full Course — ${money(p.course.launch)}</a><a href="learn.html?module=1&amp;lesson=purpose" data-resume data-resume-label="Continue your course →">Start learning first →</a></div><p class="subtle">Checkout is not open yet. You can begin with the available lessons now.</p></div><div><h4>Included with your course</h4>${list(p.course.includes)}${p.course.certificate.enabled?'<p>Certificate of completion included.</p>':`<p class="subtle">${p.course.certificate.note}</p>`}</div></div><p class="availability-note">${p.course.availability}</p></section>`;
export const suiteIntro=({showPricing=false}={})=>`<section class="suite-section" id="software-suite"><span class="eyebrow">Optional software · separate subscription</span><h2>Want to go beyond spreadsheets?</h2><p class="lead">${p.suite.name} brings digital versions of course tools together in one optional subscription. The tools are being developed to make planning, tracking, collaboration, reporting and impact management easier.</p><p class="optional-message"><b>The software subscription is optional. You do not need it to complete the course.</b> Use the Excel tools included with your course at no additional cost.</p>${showPricing?`${suitePricing()}<p>${p.suite.availability}</p><p>Tools share a subscription and design system, but each works independently. Buying the course does not activate the Suite.</p><a href="software.html">Explore the Software Suite →</a>`:''}</section>`;
const upcomingToolGroups=[
 ['Strategy & impact',[['Results & Indicator Tracking','Define indicators, data sources, collection frequency and evidence responsibilities.'],['Change Adoption Manager','Plan stakeholder engagement, readiness, communication and adoption during change.']]],
 ['Project & operations',[['Meetings, Actions & Decisions','Turn meeting notes into accountable actions and a searchable decision record.'],['Policy Management','Keep policy ownership, approvals, review dates and next actions visible.']]],
 ['Funding & Business Development',[['Funding Pipeline','Track relationship stages and distinguish opportunities from commitments.'],['Partner Mapping & Partnership Tracking','Map partnership fit, shared commitments, responsibilities and next conversations.']]],
 ['Marketing & engagement',[['Marketing Campaigns & Social Media Planner','Plan campaigns, content, channels, ownership and publishing deadlines in one place.']]]
];
export const toolAccessForm=(redirectUrl='')=>`<section class="tool-access" id="tool-access"><span class="eyebrow">Invitation access</span><h3>Have an access code?</h3><p>Enter it to unlock the Theory of Change Builder and Ethical Bridge CRM at no cost.</p><form data-tool-access-form${redirectUrl?` data-redirect-url="${redirectUrl}"`:''}><label for="access-code">Access code<input id="access-code" name="access-code" inputmode="numeric" autocomplete="one-time-code" required></label><button class="button" type="submit">Unlock the tools</button><p role="status" class="saved-status" data-tool-access-status></p></form></section>`;
export const availableToolGroups=[
 ['Strategy & impact',['strategy-kpis-annual-planning','strategic-objectives','theory-of-change']],
 ['Monitoring & evaluation',['meal-strategy']],
 ['Project & operations',['gantt','issue-risk-management']],
 ['Funding & Business Development',['donor-mapping','individual-giving']],
 ['Relationships',['ethical-bridge-crm']],
 ['People & organisation',['people-check-ins-development','onboarding-compliance','organisation-structure']],
 ['Communications, marketing & visibility',['customer-persona']]
];
const availableToolCard=tool=>tool.previewOnly
 ? `<article class="suite-tool-card"><h4>${tool.name}</h4><p>${tool.description}</p><span class="small-tag">Interactive preview</span><p class="tool-card-actions"><a class="button ghost" href="${tool.launchUrl}">Open ${tool.name} →</a></p><p class="subtle">Saved in this browser. Do not enter confidential records.</p></article>`
 : `<article class="suite-tool-card"><h4>${tool.name}</h4><p>${tool.description}</p><span class="small-tag">Available now</span><p class="tool-card-actions"><a class="button ghost" data-tool-access-link data-preserve-label data-tool-name="${tool.name}" data-tool-destination="${tool.launchUrl}" href="#tool-access">Get tool access →</a></p><p><a href="software-${tool.slug}.html">View demo and access options →</a></p></article>`;
export const softwareCatalogue=()=>`<section id="software-catalogue" class="software-catalogue"><span class="eyebrow">Impact tools</span><h2>Tools you can use now.</h2><p>Browse the tools by topic. The Theory of Change Builder and Ethical Bridge CRM use invitation access; the other browser-based tools are development previews and are not secure multi-user services.</p>${toolAccessForm()}<div class="suite-tool-groups">${availableToolGroups.map(([topic,slugs],index)=>{const tools=slugs.map(slug=>softwareProducts.find(x=>x.slug===slug)).filter(Boolean).sort((a,b)=>topic==='People & organisation'?(a.slug==='organisation-structure'?-1:b.slug==='organisation-structure'?1:a.name.localeCompare(b.name,'en')):a.name.localeCompare(b.name,'en'));return `<section class="suite-tool-topic" aria-labelledby="tool-topic-${index}"><h3 id="tool-topic-${index}">${topic}</h3><div class="suite-tool-grid">${tools.map(availableToolCard).join('')}</div></section>`}).join('')}</div><section class="upcoming-tools" aria-labelledby="upcoming-tools-title"><span class="eyebrow">In development</span><h3 id="upcoming-tools-title">Upcoming impact tools</h3><p>These product areas are planned for the Suite. They are not available yet; scope, release sequence and availability are still being confirmed.</p><div class="upcoming-tool-groups">${upcomingToolGroups.map(([topic,tools])=>`<section class="upcoming-tool-topic"><h4>${topic}</h4><div class="upcoming-tool-grid">${tools.map(([name,description])=>`<article><span class="small-tag neutral-tag">Coming soon</span><h5>${name}</h5><p>${description}</p></article>`).join('')}</div></section>`).join('')}</div></section>`;
export const conversionOptions=()=>`<section class="conversion-options" id="purchase-options"><span class="eyebrow">Choose what helps you build</span><h2>Learn the methodology.<br>Use the tools. Build better impact.</h2><div class="purchase-grid"><article class="purchase-card"><span class="eyebrow">Learn + apply</span><h3>Course</h3><strong class="option-price">${money(p.course.launch)} <small>once</small></strong><p>${p.course.name}. Lifetime access, course updates and included Excel resources.</p><a class="button" href="purchase.html?offer=course">Get the Course</a></article><article class="purchase-card"><span class="eyebrow">Scale · optional</span><h3>Software</h3><strong class="option-price">${money(p.suite.monthly)}<small>/month</small></strong><p>or ${money(p.suite.annual)}/year <span class="small-tag">Save ${annualSaving()}%</span></p><p>${p.suite.name}. No course purchase required.</p><a class="button ghost" href="software.html">Explore the Software</a></article><article class="purchase-card bundle-card"><span class="small-tag">${p.bundle.badge}</span><h3>Course + Software</h3><del>${money(bundleReference())} value</del><strong class="option-price">Launch bundle: ${money(p.bundle.launch)}</strong><p>${p.bundle.name}</p>${list(p.bundle.includes)}<p class="subtle">Save ${money(bundleSaving())} compared with the launch course plus a separately purchased annual plan. Reference value uses the regular course price plus ${p.bundle.softwareMonths} monthly software payments.</p><a class="button ghost" href="purchase.html?offer=bundle">Get the Bundle</a></article></div><p class="subtle">Launch bundle experiment · ${p.bundle.renewalNote} Checkout is not active; no payment is taken.</p></section>`;
