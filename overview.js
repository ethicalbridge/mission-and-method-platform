import {readState,stats} from './learning-store.js';
try{const state=readState();const [module,...lesson]=state.lastLesson.split('.');if(state.updatedAt)document.querySelectorAll('[data-resume]').forEach(a=>{a.href=`learn.html?module=${module}&lesson=${lesson.join('.')}`;a.textContent='Continue course preview →';});const el=document.querySelector('#account-progress');if(el){const s=stats(state);el.textContent=`${s.percent}% complete · ${s.done} of ${s.total} lessons. Saved on this browser.`;}}catch{const el=document.querySelector('#account-progress');if(el)el.textContent='Saved progress is unavailable. Open Your Organisation to restore a backup.';}
